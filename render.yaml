services:
  - type: web
    name: whatsapp-agent
    env: node
    buildCommand: npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: data
      mountPath: /opt/render/project/src/sessions
      sizeGB: 1
